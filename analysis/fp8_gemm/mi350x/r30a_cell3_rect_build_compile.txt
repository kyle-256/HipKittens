===== R30 cell=3 rect-build compile sanity =====
md5(.so) = ef0ae9a5b9d4cb79b20ae19ea3f2d5de
build flags: -DM_DIM=4096 -DN_DIM=1024 -DK_DIM=8192 -DMXFP8_RECT_BLK_N=64

PURPOSE: confirm scaffolding still compiles cleanly + scale guard fires correctly
when MXFP8_RECT_BLK_N=64 (rect mode). The R29 dispatcher guard prints stderr and
early-returns from `gemm_crr_pq_v2` to prevent the GPU memory fault that would
otherwise occur (V2 dispatch falling through to V1 layout with V2-preshuffled
scales). The V1 path (`gemm_crr_pq` + V1-preshuffled scales) handles the rect
shape correctly per R29 Cell 2 (SNR 49.59, det 3/3, 2.66 TFLOPS — 294x slower).

VERDICT: COMPILE OK. Functional behavior unchanged from R29 (V1 fallback works,
V2 fastpath force-disabled by MXFP8_CRR_EXACT_8WAVE_FAST_ENABLE=0 gate, host
guard prevents fault).
